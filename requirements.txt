# ============================================================
# 한국어 멀티모달 DPO 수집기 — 의존성
# 검증 환경: Python 3.11 / CUDA 12.6 / RTX A6000 48GB
# ============================================================

# ── PyTorch ──
# CUDA 빌드는 환경마다 달라 여기서 고정하지 않습니다.
# 먼저 https://pytorch.org 안내대로 설치하세요. 검증 버전: torch 2.7.1+cu126
#   예) pip install torch==2.7.1 torchvision --index-url https://download.pytorch.org/whl/cu126
torch>=2.5
torchvision>=0.20          # InternVL dynamic tiling 전처리에 필요

# ── 모델 로드 / 학습 ──
transformers>=4.56         # dtype 인자 지원. 검증: 5.14.1
peft>=0.15                 # LoRA. 검증: 0.19.1
accelerate>=1.0
huggingface-hub>=0.30
timm                       # InternVL 커스텀 코드가 요구
einops                     # 〃
sentencepiece
protobuf

# ── 수집 UI ──
gradio>=5.0                # 검증: 6.20.0
pandas>=2.0                # 학습 곡선/이력 표

# ── KG 구축 ──
networkx>=3.0
pillow>=10.0
pyyaml>=6.0
ruamel.yaml>=0.18          # 설정 저장 시 주석 보존 (없으면 주석이 사라집니다)

# ── 평가 / 내보내기 ──
datasets>=3.0              # 공유 포맷 검증 (load_dataset)

# ── 선택 ──
# sentence-transformers    # config 에서 semantic/cross_document 를 켤 때만 필요
# flash-attn               # use_flash_attn: true 로 쓸 때만. 없으면 자동 폴백
